# Generated by the Vibe Code factory. Do not edit by hand.
#
# One Blueprint holds every app the factory has built. A run appends its
# app's resources here, and Render's Blueprint sync deploys them.
# Apps: 16

services:
  # ── anurag/marin-silent-retreat ────────────────────
  - type: web
    name: vibe-anurag-marin-silent-retreat-web
    runtime: static
    rootDir: apps/anurag/marin-silent-retreat
    buildCommand: mkdir -p dist && cp *.html style.css dist/ && cp -r assets dist/
    staticPublishPath: dist
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/anurag/marin-silent-retreat/**
  # ── clumsy-plum-skunk/gpu-price-tracker ────────────────────
  - type: web
    name: airo-clumsy-plum--gpu-price-tracker-web
    runtime: static
    rootDir: apps/clumsy-plum-skunk/gpu-price-tracker
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/clumsy-plum-skunk/gpu-price-tracker/**
  # ── demo-user/bark-brigade ────────────────────
  - type: web
    name: vibe-demo-user-bark-brigade-web
    runtime: static
    rootDir: apps/demo-user/bark-brigade
    buildCommand: mkdir -p dist && cp index.html styles.css script.js recruits.js dist/ && cp -r assets dist/
    staticPublishPath: dist
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/bark-brigade/**
  # ── demo-user/el-fuego-tacos ────────────────────
  - type: web
    name: vibe-demo-user-el-fuego-tacos-web
    runtime: static
    rootDir: apps/demo-user/el-fuego-tacos
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/el-fuego-tacos/**
  # ── demo-user/ironridge-meatpacking ────────────────────
  - type: web
    name: vibe-demo-user-ironridge-meatpacking-web
    runtime: static
    rootDir: apps/demo-user/ironridge-meatpacking
    buildCommand: mkdir -p dist && cp index.html about.html products.html wholesale.html quality.html contact.html styles.css common.js products.js dist/ && cp -r assets dist/ && cp -r specs dist/
    staticPublishPath: dist
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/ironridge-meatpacking/**
  # ── demo-user/ironvale-meats ────────────────────
  - type: web
    name: vibe-demo-user-ironvale-meats-web
    runtime: static
    rootDir: apps/demo-user/ironvale-meats
    buildCommand: echo 'no build required — static HTML/CSS/JS'
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/ironvale-meats/**
  # ── demo-user/masa-fuego-tacos ────────────────────
  - type: web
    name: vibe-demo-user-masa-fuego-tacos-web
    runtime: static
    rootDir: apps/demo-user/masa-fuego-tacos
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/masa-fuego-tacos/**
  # ── demo-user/mendocino-silent-yoga ────────────────────
  - type: web
    name: vibe-demo-user-mendocino-silent-yoga-web
    runtime: static
    rootDir: apps/demo-user/mendocino-silent-yoga
    buildCommand: mkdir -p dist && cp *.html style.css dist/ && cp -r assets dist/
    staticPublishPath: dist
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/mendocino-silent-yoga/**
  # ── demo/gopher-gallery ────────────────────
  - type: web
    name: airo-demo-gopher-gallery-web
    runtime: static
    rootDir: apps/demo/gopher-gallery
    buildCommand: echo 'no build needed'
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo/gopher-gallery/**
  # ── demo/happy-paws ────────────────────
  - type: web
    name: airo-demo-happy-paws-web
    runtime: static
    rootDir: apps/demo/happy-paws
    buildCommand: echo 'Static site — no build step required'
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo/happy-paws/**
  # ── demo/happy-paws-dog-walking ────────────────────
  - type: web
    name: airo-demo-happy-paws-dog-walking-web
    runtime: static
    rootDir: apps/demo/happy-paws-dog-walking
    buildCommand: echo "no build needed"
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo/happy-paws-dog-walking/**
  # ── fluttering-teal-ant/driftwood-surf-swap ────────────────────
  - type: web
    name: airo-fluttering-t-driftwood-surf-swap-web
    runtime: static
    rootDir: apps/fluttering-teal-ant/driftwood-surf-swap
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/fluttering-teal-ant/driftwood-surf-swap/**
  # ── official-emerald-primate/el-fuego-tacos ────────────────────
  - type: web
    name: airo-official-eme-el-fuego-tacos-web
    runtime: static
    rootDir: apps/official-emerald-primate/el-fuego-tacos
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/official-emerald-primate/el-fuego-tacos/**
  # ── relaxed-gray-butterfly/domainpricing ────────────────────
  - type: web
    name: airo-relaxed-gray-domainpricing-web
    runtime: static
    rootDir: apps/relaxed-gray-butterfly/domainpricing
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/relaxed-gray-butterfly/domainpricing/**
  # ── scared-salmon-emu/windward-sailboats ────────────────────
  - type: web
    name: airo-scared-salmo-windward-sailboats-web
    runtime: static
    rootDir: apps/scared-salmon-emu/windward-sailboats
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/scared-salmon-emu/windward-sailboats/**
  # ── yeasty-bronze-kiwi/nimbus-hosting ────────────────────
  - type: web
    name: airo-yeasty-bronz-nimbus-hosting-web
    runtime: static
    rootDir: apps/yeasty-bronze-kiwi/nimbus-hosting
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/yeasty-bronze-kiwi/nimbus-hosting/**
