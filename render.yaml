# Generated by the Vibe Code factory. Do not edit by hand.
#
# One Blueprint holds every app the factory has built, each in its own
# project. A run appends its app here, and Render's Blueprint sync
# deploys it.
# Apps: 9

projects:
  # ── anurag/marin-silent-retreat ────────────────────
  - name: vibe-anurag-marin-silent-retreat
    environments:
      - name: production
        services:
          - type: web
            name: vibe-anurag-marin-silent-retreat-web
            runtime: static
            rootDir: "apps/anurag/marin-silent-retreat"
            buildCommand: "mkdir -p dist && cp *.html style.css dist/ && cp -r assets dist/"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/anurag/marin-silent-retreat/**"
  # ── anurag/peninsula-interiors ────────────────────
  - name: vibe-anurag-peninsula-interiors
    environments:
      - name: production
        services:
          - type: web
            name: vibe-anurag-peninsula-interiors-web
            runtime: static
            rootDir: "apps/anurag/peninsula-interiors"
            buildCommand: "mkdir -p dist && cp index.html styles.css script.js dist/ && cp -r assets dist/assets"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/anurag/peninsula-interiors/**"
  # ── demo-user/bark-brigade ────────────────────
  - name: vibe-demo-user-bark-brigade
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-bark-brigade-web
            runtime: static
            rootDir: "apps/demo-user/bark-brigade"
            buildCommand: "mkdir -p dist && cp index.html styles.css script.js recruits.js dist/ && cp -r assets dist/"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/bark-brigade/**"
  # ── demo-user/el-fuego-tacos ────────────────────
  - name: vibe-demo-user-el-fuego-tacos
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-el-fuego-tacos-web
            runtime: static
            rootDir: "apps/demo-user/el-fuego-tacos"
            buildCommand: "true"
            staticPublishPath: "."
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/el-fuego-tacos/**"
  # ── demo-user/gloomy-tails ────────────────────
  - name: vibe-demo-user-gloomy-tails
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-gloomy-tails-web
            runtime: static
            rootDir: "apps/demo-user/gloomy-tails"
            buildCommand: "rm -rf dist && mkdir -p dist && cp -r index.html specialists.html symptoms.html success-stories.html resources.html contact.html css js assets dist/"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/gloomy-tails/**"
  # ── demo-user/ironridge-meatpacking ────────────────────
  - name: vibe-demo-user-ironridge-meatpacking
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-ironridge-meatpacking-web
            runtime: static
            rootDir: "apps/demo-user/ironridge-meatpacking"
            buildCommand: "mkdir -p dist && cp index.html about.html products.html wholesale.html quality.html contact.html styles.css common.js products.js dist/ && cp -r assets dist/ && cp -r specs dist/"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/ironridge-meatpacking/**"
  # ── demo-user/ironvale-meats ────────────────────
  - name: vibe-demo-user-ironvale-meats
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-ironvale-meats-web
            runtime: static
            rootDir: "apps/demo-user/ironvale-meats"
            buildCommand: "echo 'no build required — static HTML/CSS/JS'"
            staticPublishPath: "."
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/ironvale-meats/**"
  # ── demo-user/masa-fuego-tacos ────────────────────
  - name: vibe-demo-user-masa-fuego-tacos
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-masa-fuego-tacos-web
            runtime: static
            rootDir: "apps/demo-user/masa-fuego-tacos"
            buildCommand: "true"
            staticPublishPath: "."
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/masa-fuego-tacos/**"
  # ── demo-user/mendocino-silent-yoga ────────────────────
  - name: vibe-demo-user-mendocino-silent-yoga
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-mendocino-silent-yoga-web
            runtime: static
            rootDir: "apps/demo-user/mendocino-silent-yoga"
            buildCommand: "mkdir -p dist && cp *.html style.css dist/ && cp -r assets dist/"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/mendocino-silent-yoga/**"
