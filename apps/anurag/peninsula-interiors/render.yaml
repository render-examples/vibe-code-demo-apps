# anurag/peninsula-interiors — generated by the Vibe Code factory.
# Prompt: Create a website for an interior designer in the Peninsula, Bay Area

projects:
  # ── anurag/peninsula-interiors ────────────────────
  - name: vibe-anurag-peninsula-interiors
    environments:
      - name: production
        services:
          - type: web
            name: vibe-anurag-peninsula-interiors-web
            runtime: static
            rootDir: apps/anurag/peninsula-interiors
            buildCommand: mkdir -p dist && cp index.html styles.css script.js dist/ && cp -r assets dist/assets
            staticPublishPath: dist
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - apps/anurag/peninsula-interiors/**