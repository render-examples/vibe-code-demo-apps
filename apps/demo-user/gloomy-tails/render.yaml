# demo-user/gloomy-tails — generated by the Vibe Code factory.
# Prompt: create a website for depressed dogs to get help from specialists

projects:
  # ── demo-user/gloomy-tails ────────────────────
  - name: vibe-demo-user-gloomy-tails
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-gloomy-tails-web
            runtime: static
            rootDir: "apps/demo-user/gloomy-tails"
            buildCommand: "rm -rf dist && mkdir -p dist && cp -r index.html specialists.html symptoms.html success-stories.html resources.html contact.html css js assets dist/"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/gloomy-tails/**"