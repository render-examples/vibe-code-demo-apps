# demo-user/aussie-shepherd-guide — generated by the Vibe Code factory.
# Prompt: Create a website that includes information on Australian Shepherds.

projects:
  # ── demo-user/aussie-shepherd-guide ────────────────────
  - name: vibe-demo-user-aussie-shepherd-guide
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-aussie-shepherd-guide-web
            runtime: static
            rootDir: "apps/demo-user/aussie-shepherd-guide"
            buildCommand: "rm -rf dist && mkdir -p dist && cp *.html styles.css script.js dist/ && cp -r assets dist/assets"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/aussie-shepherd-guide/**"