# demo-user/synthwave-collective — generated by the Vibe Code factory.
# Prompt: Create a website for synth enthusiasts

projects:
  # ── demo-user/synthwave-collective ────────────────────
  - name: vibe-demo-user-synthwave-collective
    environments:
      - name: production
        services:
          - type: web
            name: vibe-demo-user-synthwave-collective-web
            runtime: static
            rootDir: "apps/demo-user/synthwave-collective"
            buildCommand: "mkdir -p dist && cp -r src/*.html src/*.css src/*.js dist/ && cp -r assets dist/assets"
            staticPublishPath: "dist"
            autoDeployTrigger: commit
            buildFilter:
              paths:
                - "apps/demo-user/synthwave-collective/**"