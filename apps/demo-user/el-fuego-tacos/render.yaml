# demo-user/el-fuego-tacos — generated by the Vibe Code factory.
# Prompt: Create a website for selling tacos. make it just a static site

services:
  - type: web
    name: vibe-demo-user-el-fuego-tacos-web
    runtime: static
    rootDir: apps/demo-user/el-fuego-tacos
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo-user/el-fuego-tacos/**